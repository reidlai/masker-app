---
topic: Settings page + bottom-nav Profile→Settings swap (Update)
updated: 2026-09-05T23:15
---

- (decision) Scope: Update existing final spines. Add bottom-nav tab bar to IA (backfill), swap tab 4 Profile->Settings, add MOB_SETTINGS screen, settings menu-row pattern, conditional Debugging/Developer items.
- (decision) Q1-Q4,Q7-Q9 defaults accepted: bottom NAV bar (not modal sheet); tab4 label 'Settings' + outlined gear icon; Profile reachable ONLY via Settings->Profile, no other entry points; item order Profile, Debugging, Developer; full-screen push w/ app bar back arrow, title 'Settings', back Profile->Settings->prev tab; list spec'd as extensible for future settings; conditional rows use identical styling to Profile row.
- (decision) Q5 refined (Reid): 'Advanced' section header shown ONLY when at least one of {Debugging mode, Developer mode} is enabled. If both disabled, no Advanced section and Settings page shows only the Profile row.
- (assumption) Q6 [ASSUMPTION - confirm at build/architecture]: 'Debugging mode' and 'Developer mode' are two independent booleans. Debugging gated by Flutter kDebugMode; Developer gated by --dart-define=DEV_MODE=true. Both menu items inert (no destination/action) for now.
- (change) EXPERIENCE.md v1.0.0->v1.1.0: added Primary Navigation (bottom nav bar) subsection + Settings Screen (MOB_SETTINGS) to IA; Voice&Tone bullet for Settings; Component Patterns #4 PrimaryNavBarOrganism + #5 SettingsMenuRowOrganism; State Patterns #8 State_SettingsDefault + #9 State_SettingsAdvanced; Interaction Primitive for list-row nav/back stack; 2 Accessibility Floor bullets; Secondary Flow (David corrects weight).
- (change) DESIGN.md v1.0.0->v1.1.0: components frontmatter += BottomNavBar, SettingsMenuRow, SettingsSectionHeader; Components #5 BottomNavBar, #6 SettingsMenuRow, #7 SettingsSectionHeader; +1 DO (+gate rows / hide empty Advanced header) and +1 DON'T (no chevron on inert rows).
- (event) Conflict surfaced: v1.0.0 IA + David journey treat MOB_USER_PROFILE as onboarding-only 'Setup'. Now also a persistent edit destination via Settings. Not a contradiction - screen reused; logged [NOTE FOR UX] that Profile screen may need first-run vs edit mode.
- (assumption) [ASSUMPTION] Tab 1 'Home' (MOB_HOME) has no spec in this spine - captured structurally only. Tabs 2/3 map to existing MOB_SLEEP_MONITOR / MOB_SLEEP_SUMMARY.
- (decision) Q8 (Reid): future Settings rows confirmed = Notifications, Account, Sign out, About, Caregiver contacts. Added as 'Planned rows' IA table in EXPERIENCE.md with target grouping (Account&Profile / Preferences / Advanced / About / Sign out). This iteration still ships Profile row + conditional Advanced only. 'Units' (metric<->imperial) left [UNDECIDED] pending Reid's call on imperial input support.
- (decision) Units CONFIRMED in planned rows (no longer [UNDECIDED]). Units preference (metric<->imperial, default metric) governs Weight/Height entry fields on MOB_USER_PROFILE and the BMI computation: user enters weight+height, app computes BMI in the selected system. Added cross-dependency note to the MOB_USER_PROFILE [NOTE FOR UX].
- (event) bmad-review (structure+prose lenses) run on DESIGN.md + EXPERIENCE.md v1.1.0. 13 findings (7 structure, 6 prose).
- (change) Applied 11 review fixes: merged Debugging/Developer row bullets + dropped tree [ASSUMPTION] tags; merged nav-table cell with Change-from-v1.0.0 paragraph; US spelling (labelled->labeled, behaviour->behavior x2); slash conjunctions -> 'or'/comma; 'It renders only when X or Y'; [NOTE FOR UX] -> 2-item list; Secondary Flow: added start tab 'Home', dropped hardcoded profile numbers; Voice&Tone 'single words' -> 'short, one word where possible'; DESIGN BottomNavBar: cut behavioral/IA-change clauses; added colors.pressed_surface token (#273449) to frontmatter+table, wired SettingsMenuRow to it; dropped filler 'actually'. Skipped (low-confidence): relocating Planned-rows section.
- (event) spines finalized
- (decision by user) Scope: Update existing final spines (v1.1.0) to cover 2 gaps from PRD FR-1.11 / Architecture AD-12 (App-Boot Background BLE Receiver & RxDart Reactive Streaming Queue), routed via sprint-change-proposal-2026-09-04.md item 3.3: (1) Android Foreground Service persistent notification, (2) OS Bluetooth background-permission priming.
- (decision by user) Priming screen APPROVED over bare OS prompt: new screen MOB_BLE_PERMISSION_PRIMER explains why Bluetooth access is needed before the native OS permission dialog fires. Rationale: regulated health app, bare OS permission text alone reads alarming with zero context.
- (decision by user) Timing APPROVED: end of onboarding, right before first boot. MOB_BLE_PERMISSION_PRIMER inserted into the flow between MOB_USER_PROFILE and MOB_DEVICE_PAIRING, framed as 'one more step to enable overnight monitoring.' First-run only \u2014 skipped on all subsequent launches once permission is granted, so the AD-12 receiver Foreground Service can start on schedule from the next boot onward.
- (decision by user) Notification tap APPROVED: opens the app to its current screen (MOB_SLEEP_MONITOR if a session is active, else last foregrounded screen) \u2014 standard Android FGS behavior.
- (decision by user) Notification copy direction APPROVED: reassuring/status. Title 'Sleep Monitoring Ready', Body 'D-BAND connection active \u2014 you're covered tonight.' Sits in the same Voice&Tone bucket as onboarding copy.
- (assumption) [ASSUMPTION] OS-permission-denied recovery path not elicited from Reid \u2014 defaulted to the standard mobile pattern: if the OS Bluetooth permission is denied, MOB_DEVICE_PAIRING renders a blocked state (no scan UI) with an 'Open Settings' deep-link CTA to the device's app-permission settings, mirroring the existing wear-guardrail block pattern (Story 2.3). Confirm at build.
- (assumption) [ASSUMPTION] FGS notification treated as 'ongoing' (Android) \u2014 not swipe-dismissible while the receiver service is resident, matching AD-12 'queue survives for next session.' Status-bar icon is a small monochrome glyph per platform convention, not the full-color launcher icon. Confirm exact asset at build.
- (change) EXPERIENCE.md v1.1.0->v1.2.0: flow map gains MOB_BLE_PERMISSION_PRIMER (first-run only) between MOB_USER_PROFILE and MOB_DEVICE_PAIRING; new IA subsection 'Onboarding: Bluetooth Background-Access Priming'; Voice&Tone gains approved priming + notification copy; Component Patterns #7 BlePermissionPrimerOrganism, #8 BleReceiverForegroundNotification; State Patterns #10 State_BlePermissionPriming, #11 State_BlePermissionDenied; Interaction Primitive 'OS Permission Hand-off'; 2 Accessibility Floor bullets; Primary Flow (David) renumbered with new step 2 (priming, first-run only) and notification beat added to Night Mode step.
- (change) DESIGN.md v1.1.0->v1.2.0: components frontmatter += BlePermissionPrimerCard; Components #9 BlePermissionPrimerCard; +1 DO (frame OS permission dialogs with in-app context first) and +1 DON'T (don't let the persistent notification interrupt/light up the screen mid-session beyond the standard OS tap-to-open).
- (event) Reviewer Gate run: rubric walker (review-rubric.md, 21 findings: 3 critical/6 high/6 medium/6 low) + ad-hoc accessibility lens (review-accessibility.md, 3 critical/3 high/5 medium/3 low), both on the new BLE-priming/notification content plus the wider spine pair.
- (change) Fixed all accessibility findings scoped to the new BLE-priming/notification content: (1) corrected binary-permission-dialog claim, Android shows BLUETOOTH_SCAN+BLUETOOTH_CONNECT sequentially; added State_BlePermissionPartial. (2) added BleNotProtectedNotification + State_BleNotProtected as a positive 'not protected' signal, not just absence-of-notification. (3) iOS one-shot dialog handling: primer checks prior-denial state, routes to Settings deep-link instead of re-tapping a dead dialog. (4) added neverForLocation manifest note. (5) authored NSBluetoothAlwaysUsageDescription copy in Voice&Tone. (6) defined default primary ShadButton color pairing (accent_green fill / primary text, ~7.8:1) in DESIGN.md #10, used by the primer CTA. (7) fixed 'reachable' overclaim -> programmatic initial a11y focus on headline. (8) marked Bluetooth glyph icon decorative. (9) cross-referenced the notification Do/Don't to scope 'no re-lighting' to notification-initiated only, not user tap-to-open. (10) renamed BlePermissionPrimerCard -> BlePermissionPrimerOrganism in DESIGN.md to match EXPERIENCE.md (name-drift fix, scoped to this pass's own new component).
- (decision by user) Fixed now (Reid, out-of-scope-but-tightly-coupled call): Night Mode accessible-label gap. State_MonitoringActive / LiveAirflowMonitorOrganism now specify a persistent accessible label ('Sleep monitoring active - D-BAND connected') on the root semantics node even though nothing renders visually, plus a new Accessibility Floor bullet. This touches pre-existing v1.1.0 spec, approved as an exception because it's the same 'is monitoring active' concern this whole pass is about.
- (event) Non-blocking findings logged, NOT fixed this pass (pre-existing debt predating this update, or low-severity/judgment calls) -- kept this update scoped to BLE priming/notification: (a) MOB_HISTORY_FILTER and MOB_EXPORT_DOCTOR named only in the flow-map ASCII diagram, never specified elsewhere. (b) Component coverage broken: LiveAirflowMonitorOrganism has no DESIGN.md visual row; MetricStatCard/SessionHistoryListItem have no EXPERIENCE.md behavioral row; name drift BottomNavBar/PrimaryNavBarOrganism, ApneaAlertBanner/ApneaAlertBannerOrganism, SettingsMenuRow/SettingsMenuRowOrganism (pre-existing, not this pass's new BLE components which were fixed). (c) No offline/network-failure state for the Tier-1 alarm's cloud safety packet. (d) No mid-session BLE disconnect/recovery flow during State_MonitoringActive (adjacent to AD-12 but not specified). (e) 3 existing mockups (mob_passkey_auth, mob_sleep_summary, mob_tier1_alarm) are orphaned -- not linked inline from either spine. (f) MOB_DEVICE_PAIRING missing scanning/paired/timeout states beyond the new permission states. (g) MOB_USER_PROFILE first-run-vs-edit-mode [NOTE FOR UX] still unresolved into formal state. (h) Emergency alert flash rate/duty-cycle unspecified (WCAG 2.3.1 photosensitivity risk). (i) No alternate motor-accessibility path for Tier-1 'I'm Safe' single-tap dismiss beyond auto-silence-on-recovery. (j) Observation: BLE priming screen lands at 10:20pm bedtime, arguably better suited to daytime initial setup -- worth reconsidering flow placement if there's flexibility.
- (event) bmad-review (structure+prose lenses) run on the updated DESIGN.md + EXPERIENCE.md. Structure: 4 CONDENSE findings (redundant near-duplicate passages introduced while fixing accessibility findings) + 2 QUESTIONs. Prose: 9 findings against Microsoft Writing Style Guide (3 firm, 6 Consider).
- (change) Applied all 4 structure CONDENSE fixes (Denial/partial-grant recovery bullet now points to State Patterns #11/#12 instead of restating; OS Permission Hand-off primitive condensed to a cross-reference; BlePermissionPrimerOrganism opening clause condensed; DESIGN.md DON'T parenthetical condensed) + the BleNotProtectedNotification copy-duplication QUESTION resolved by cross-referencing Voice&Tone instead of re-quoting. Left the screen-dossier-depth QUESTION as-is (defensible: the 2 flagged screens are the newest/most complex). Applied all 9 prose findings (e.g.->for example x3, colon-capitalization consistency, instead/rather-than redundancy, 'on screen appear' grammar, +/and register mismatch x2, decorative/excluded slash, (below)/pinned directional-word clash, fix-this/toward-alarm parallelism, rather-than-after ellipsis, 'On this, his very first night' rephrase).
- (event) spines finalized
- (decision by user) Scope: render mockups for all 12 specified IA surfaces (all except MOB_HOME, which has no spec yet - [ASSUMPTION] tab exists, nothing decided about its content). Screens: MOB_PASSKEY_AUTH, MOB_USER_PROFILE, MOB_BLE_PERMISSION_PRIMER, MOB_DEVICE_PAIRING, MOB_CALIBRATION_STAGE1, MOB_CALIBRATION_STAGE2, MOB_SLEEP_MONITOR, MOB_TIER1_ALARM, MOB_SLEEP_SUMMARY, MOB_GRAPH_WAVEFORM, MOB_HISTORY_FILTER, MOB_EXPORT_DOCTOR, MOB_SETTINGS. Reason: Reid wants the full mockup set kept in GitHub as a verification reference with users, not just the 2-4 load-bearing surfaces the key-screens tool defaults to.
- (decision by user) Visual direction APPROVED for all mockups: Android device chrome (not the previous generic rounded phone bezel) - Pixel-style rectangular device silhouette, Material-style Android status bar (time left, signal/wifi/battery icons right, light icons on the dark theme), gesture-pill nav bar at the bottom of every screen. NOT full AVD emulator window chrome (no side power/volume buttons, no camera-cutout hardware bezel, no device-name label) - screen content + Android system bars only.
- (decision by user) The existing 3 mockups (mob_passkey_auth.html, mob_sleep_summary.html, mob_tier1_alarm.html) are being regenerated in the new Android chrome style for visual consistency across the full 12-screen set, replacing their prior generic-phone-bezel versions.
- (event) Built a Penpot flow prototype from the 13 rendered mockups, per Reid's request to keep a shareable/clickable design artifact alongside DESIGN.md/EXPERIENCE.md. Approach: self-hosted Penpot (Docker, localhost:9001) + Penpot MCP server (npx @penpot/mcp@2.17.0, local process) + Penpot MCP Plugin (loaded in-app, bridges the editor to the MCP server). Each of the 13 mockup HTML files was screenshotted at device resolution (headless Chrome) and imported as an image-filled Board via the import_image MCP tool (not native editable vector shapes - mockups stay the source of truth for exact spacing/copy). Boards laid out left-to-right in IA flow order and wired with click-triggered navigate-to interactions (Board 1 -> 2 -> ... -> 13), plus a named Flow ('Full App Walkthrough') starting at Board 1, so Penpot's View mode gives a clickable walkthrough of the whole app. File: 'Sleep Apnea App - Mockup Flow' in Reid's Penpot Drafts. MOB_HOME excluded (no spec yet, per earlier decision).
- (change) EXPERIENCE.md v1.2.0 -> v1.2.1: added a 'Clickable prototype' note directly under the Screen Navigation & Flow Map, linking to the Penpot flow file (localhost, requires the self-hosted stack running) and to mockups/*.html as the static fallback reference. Decided: link EXPERIENCE.md -> Penpot one-way (not vice versa, since Penpot boards are images, not a place to maintain reciprocal doc links).
- (change) Penpot flow file: closed the 80px inter-board gap to 0 per Reid's request - all 13 boards now sit edge-to-edge in flow order (board N's right edge = board N+1's left edge, verified numerically to sub-pixel precision). Confirmed via inspection that board/rectangle/fillImage dimensions were already exact matches (410x854, matching source PNG native resolution) - the visible gap Reid noticed was intentional inter-board spacing, not a size mismatch, and is now removed.
- (decision by user) Reid rejected edge-to-edge Penpot board spacing and rejected the Android device chrome. Switched standard artboard to iPhone 17 Pro Max (440x956pt logical resolution, verified against Apple's published specs) with a proper Dynamic Island, iOS-style status bar (signal/wifi/battery icons in Apple's style), and iOS home indicator bar - replacing the earlier Android Material chrome (status bar + gesture pill). Also addressed Reid's 'artboard not large enough to centralize UI component' complaint: all 13 mockups redrawn with improved internal centering/spacing to use the ~13% larger canvas deliberately, not just stretched into a bigger box with more empty space. Copy preserved verbatim from the prior pass throughout.
- (change) Rebuilt the Penpot flow file end-to-end: removed all 13 old (Android-chrome, 410x854) boards, re-screenshotted all 13 redrawn mockups at the new iPhone size, re-imported and re-wrapped as boards, re-wired all 12 click-to-navigate interactions plus the 'Full App Walkthrough' Flow. Board spacing set to 120px gap (up from the original 80px, and explicitly not 0/edge-to-edge per Reid's correction). Caught and fixed a real bug during rebuild: mob_device_pairing.html's two-frame side-by-side layout (~928px content width) was initially screenshotted at only 460px width, clipping the second (blocked-state) panel entirely - re-captured at the correct 960x996 before importing.
- (event) Root-caused the right-alignment bug Reid saw in Penpot: Penpot boards drag their children along automatically when the board's own x/y is set (group-like behavior) - my repeated delta-based repositioning code (edge-to-edge pass, then grid pass) was ALSO manually shifting each child by the same delta on top of that automatic drag, double-applying every move and compounding across passes (errors as large as several thousand px by the third pass). Fixed by abandoning delta math entirely and using penpotUtils.setParentXY(child, 0, 0) on every board's child after final positioning - the documented, board-relative way to position a child regardless of the board's own absolute position. All 13 boards verified correct via server-side export_shape after the fix. Also rebuilt the layout as a 4-column x 4-row grid (was a single 13-wide row) per Reid's request to see more screens at once without deep horizontal scrolling/zooming - IA/flow order preserved reading left-to-right then top-to-bottom, all 12 navigate-to interactions and the Full App Walkthrough Flow unaffected by the regrid.
- (change) Fixed off-center mockup screenshots: root cause was headless Chrome CLI silently flooring --window-size width at 500px, cropping an already-centered render off-center. Rebuilt capture via Puppeteer (single render pass, measured device-frame bbox, symmetric 20px margin crop). Regenerated all 13 PNGs in .working/screenshots/ (480x996; device_pairing 968x1026 incl. column captions). Re-imported all 13 into Penpot file 'Sleep Apnea App - Mockup Flow' (aeeec736-5c79-81d3-8008-9719456b8880); resized each board+rect to match its PNG exactly (zero scaling distortion). Verified 7/13 boards via server-side export_shape - all symmetric.
